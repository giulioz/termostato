#ifndef CONNECTION_HPP
#define CONNECTION_HPP

namespace Connection {

void init();
void connectAndReboot(const char* ssid, const char* password);

}  // namespace Connection

#endif  // CONNECTION_HPP
